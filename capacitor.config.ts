import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.newdelta.app',
  appName: 'New Delta',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
